{% extends 'base.html.twig' %}

{% block title %}Tamagosaurus index{% endblock %}

{% block body %}
    <h1>Tamagosaurus index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Hunger</th>
                <th>Name</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for tamagosauru in tamagosauruses %}
            <tr>
                <td>{{ tamagosauru.id }}</td>
                <td>{{ tamagosauru.hunger }}</td>
                <td>{{ tamagosauru.name }}</td>
                <td>
                    <a href="{{ path('app_tamagosaurus_show', {'id': tamagosauru.id}) }}">show</a>
                    <a href="{{ path('app_tamagosaurus_edit', {'id': tamagosauru.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_tamagosaurus_new') }}">Create new</a>
{% endblock %}
