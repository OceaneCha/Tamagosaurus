{% extends 'base.html.twig' %}

{% block title %}Tamagosaurus
{% endblock %}

{% block body %}
	<h1>Tamagosaurus</h1>
	<div>
		{% include('login/index.html.twig') %}
	</div>

	<table class="table">
		<tbody>
			<tr>
				<th>Id</th>
				<td>{{ tamagosauru.id }}</td>
			</tr>
			<tr>
				<th>Hunger</th>
				<td>{{ tamagosauru.hunger }}</td>
			</tr>
			<tr>
				<th>Name</th>
				<td>{{ tamagosauru.name }}</td>
			</tr>
		</tbody>
	</table>

	<a href="{{ path('app_tamagosaurus_index') }}">back to list</a>

	<a href="{{ path('app_tamagosaurus_edit', {'id': tamagosauru.id}) }}">edit</a>

	{# {{ include('tamagosaurus/_delete_form.html.twig') }}#}
{% endblock %}
